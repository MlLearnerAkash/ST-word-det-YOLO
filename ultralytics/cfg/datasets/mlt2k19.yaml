# Dataset configuration for YOLO model training
path: /home/akash/ws/dataset/ST/MLT2k19/mlt2k19/YOLOData
train: images/train  # Path to the directory containing training images
val: images/val      # Path to the directory containing validation images

# Number of classes in the dataset
nc: 1  # You have one class (label `0` for all annotations)

# Class names
names:
  0: w  # Replace "object" with a meaningful name if applicable
